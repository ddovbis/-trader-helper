TODO: Add requirements.txt

The following dependencies should be installed manually:
1. ta-lib (https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib.) 	
	- for windwos — requires the wheel to be downloaded from the source above;
	- for linux — might work with pip; if not, should be installed from the same source. 